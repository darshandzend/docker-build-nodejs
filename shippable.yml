script:
  - true

env:
  global:
    - APP_NAME=shippable-heroku-test

after_success :
  - pwd
  - ls
  - whoami
  - find / -iname "*.git"
  - git push -f git@heroku.com:shippable-heroku-test.git master
